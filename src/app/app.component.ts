import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <main-menu></main-menu>
    <router-outlet></router-outlet>
  `,
  styles: [`

  `]
})
export class AppComponent {
}
